parameters:
  openshift4_oauth:
    namespace: openshift-config

    identityProviders: []

    templates:
      # `error` has a speacial meaning for jsonnet. Using `err` isntead.
      err: ''
      login: ''
      providerSelection: ''

    token:
      timeoutSeconds: ~
      maxAgeSeconds: ~

    ldapSync:
      schedule: '*/5 * * * *'
