parameters:
  openshift4_oauth:
    namespace: openshift-config

    identityProviders: []

    templates:
      err: ''
      login: ''
      providerSelection: ''

    token:
      timeoutSeconds: ~
      maxAgeSeconds: ~
